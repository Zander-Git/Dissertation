psudo code ideas

import
create empty matrix
resample songs
add to group type matrix
repeat for other type
half length of tracks(?)
find correlation
make presentable



import all 
create empty matrixs
loop: resample all songs
loop: add all to own group matrix
	this includes recorded different scenarios	
loop: file-file, file-recorded, recorded-recorded

loop through scenarios(5)
	loop through filters (10)
		loop through tracks (5)
		apply filter(i)
			corr against file
			corr against recorded
			performance

loop apply math (diff & diff/performance)
wrap up
write to txt